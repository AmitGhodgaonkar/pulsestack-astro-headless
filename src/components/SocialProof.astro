---
const { socialProof } = Astro.props;
---
<section class="border-b border-slate-800 bg-slate-950/80">
  <div class="mx-auto max-w-6xl px-4 py-8">
    <p class="text-center text-xs uppercase tracking-[0.2em] text-slate-500 mb-4">
      {socialProof.heading}
    </p>
    <div class="flex flex-wrap justify-center gap-6 text-xs text-slate-400">
      {socialProof.logos.map((logo) => (
        <div class="px-3 py-1 rounded-full border border-slate-800/80 bg-slate-900/60">
          {logo}
        </div>
      ))}
    </div>
  </div>
</section>
