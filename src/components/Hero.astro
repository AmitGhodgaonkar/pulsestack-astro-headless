---
const { hero } = Astro.props;
---

<section class="mx-auto max-w-6xl px-4 pb-16 pt-10 md:pt-16 grid gap-10 md:grid-cols-[minmax(0,1.1fr)_minmax(0,1fr)] items-center">
  <div class="space-y-6">
    <div class="inline-flex items-center gap-2 rounded-full border border-slate-700 bg-slate-900/60 px-3 py-1 text-xs text-slate-300">
      <span class="h-1.5 w-1.5 rounded-full bg-emerald-400 animate-pulse" />
      <span>{hero.eyebrow}</span>
    </div>
    <h1 class="text-3xl md:text-5xl font-semibold tracking-tight text-slate-50">
      {hero.title}
    </h1>
    <p class="text-slate-300 text-sm md:text-base max-w-xl">
      {hero.subtitle}
    </p>
    <div class="flex flex-col sm:flex-row gap-3">
      <a
        href={hero.primaryCta.href}
        class="inline-flex items-center justify-center rounded-full bg-indigo-500 px-6 py-2.5 text-sm font-medium text-slate-950 shadow-lg shadow-indigo-500/30 hover:bg-indigo-400 transition"
      >
        {hero.primaryCta.label}
      </a>
      <a
        href={hero.secondaryCta.href}
        class="inline-flex items-center justify-center rounded-full border border-slate-700 px-6 py-2.5 text-sm text-slate-100 hover:border-indigo-400 hover:text-indigo-100 transition"
      >
        {hero.secondaryCta.label}
      </a>
    </div>
    <p class="text-xs text-slate-400">
      No credit card required · 14-day full access · Cancel anytime
    </p>
  </div>

  <div class="relative">
    <div class="rounded-2xl border border-slate-800 bg-slate-900/80 p-4 text-xs text-slate-200">
      <p class="text-[11px] text-slate-400 mb-2">Example AI insight</p>
      <p class="text-sm mb-2">
        “Churn risk is highest for teams with 5–10 seats and no onboarding guidance.”
      </p>
      <div class="flex gap-3 text-[11px] text-slate-400">
        <span>Source: Support, NPS, Sales notes</span>
        <span>Impact: High</span>
      </div>
    </div>
  </div>
</section>
